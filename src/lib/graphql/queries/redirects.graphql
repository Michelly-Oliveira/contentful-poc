query GetAllRedirects($limit: Int = 100, $skip: Int = 0) {
  redirectCollection(limit: $limit, skip: $skip) {
    total
    items {
      sys {
        id
      }
      source
      destination
      permanent
    }
  }
}
